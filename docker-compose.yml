services:
  backend:
    build:
      context: ./backend
    develop:
      watch:
        - path: ./backend/src
          action: sync
          target: /app/src
        - path: ./backend/uv.lock
          action: rebuild
    ports:
      - 8765:8000
    tty: true
    environment:
      - DATABASE_USER=admin
      - DATABASE_PASSWORD=admin
      - DATABASE_HOST=postgres
      - DATABASE_PORT=5432
      - DATABASE_DB=hiremepls
  postgres:
    image: postgres:17
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: hiremepls
    volumes:
      - db_data:/var/lib/postgresql/data:rw
volumes:
  db_data: